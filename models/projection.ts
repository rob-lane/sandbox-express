// Can we infer the type of this file from the prisma model/schema definition?

export interface Projection { 
    name: string;
    type: string;
    unit: string;
    yearType: string;
    year: number;
    value: number;
}